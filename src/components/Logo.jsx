import React from 'react'

const Logo = ({className=""}) => {
  return (
    <div>
      <h1 className={`text-2xl font-extrabold ${className}`}> HomeServify</h1>
    </div>
  )
}

export default Logo
